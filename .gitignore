# Credentials — never commit
.env

# macOS metadata
.DS_Store

# Log files
logs/

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/
dist/
build/
.eggs/

# Runtime state (optional to track — contains no secrets,
# but it's machine-local so usually excluded)
state/
